language: python
python:
  - "3.5"
# command to install dependencies
install:
  - pip install --upgrade pip
  - pip install flask spotipy pandas numpy scikit-learn pytest
# command to run tests

script:
  - python train.py



